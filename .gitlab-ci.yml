image: docker:latest

services:
  - docker:dind


stages:
  - build
  - deploy-dev


variables:
  DOCKER_DRIVER: overlay2
  DOCKER_HOST: tcp://localhost:2375
  TEST_IMAGE: code.graysonhead.net:5555/graysonhead/navplan:$CI_COMMIT_REF_NAME
  KUBECONFIG: /etc/deploy/config


before_script:
  - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN code.graysonhead.net:5555

build:
  stage: build
  image: docker:dind
  script:
    - docker build --pull -t $TEST_IMAGE .
    - docker push $TEST_IMAGE

deploy-dev:
  stage: deploy-dev
  only:
    - development
  image: lwolf/helm-kubectl-docker:v1.10.13-v2.12.3
  before_script:
    - mkdir -p /etc/deploy
    - export KUBECONFIG=${KUBECONFIG}
    - echo ${kube_config} | base64 -d > ${KUBECONFIG}
  script:
    - kubectl get nodes
    - kubectl apply -f ./kube_deployment/navplan-deployment.yaml --kubeconfig=${KUBECONFIG}